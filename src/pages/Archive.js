// === IMPORTS ============================
// components
import PageTemplate from "../components/PageTemplate"

export default function Archive() {
    return (
        <PageTemplate>
            <h1>Archive</h1>
        </PageTemplate>
    )
}